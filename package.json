{
  "name": "grievance-platform",
  "version": "1.0.0",
  "private": true,
  "description": "AI Powered Enterprise Citizen Service & Grievance Management Platform — API specification, OpenAPI 3.1 contract, and generated Postman collection.",
  "scripts": {
    "validate:spec": "node scripts/validate-openapi.js",
    "lint:openapi": "redocly lint docs/openapi.yaml",
    "validate": "npm run validate:spec && npm run lint:openapi",
    "postman:ci": "npm ci --prefix postman",
    "postman:build": "npm run build --prefix postman",
    "build": "npm run validate && npm run postman:ci && npm run postman:build"
  },
  "license": "SEE LICENSE IN LICENSE",
  "devDependencies": {
    "@redocly/cli": "^1.0.0",
    "js-yaml": "^4.1.0"
  }
}
