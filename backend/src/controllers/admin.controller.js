'use strict';

// HTTP-layer handlers for the Administration module: parse/validate request,
// call src/services/admin.service.js, shape the response via
// src/utils/apiResponse.js. No business logic lives here or in the service
// yet — populated in the implementation phase.
module.exports = {};
