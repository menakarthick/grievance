'use strict';

// express-validator chains for the Notification module's request bodies,
// params, and query strings, run through src/middleware/validate.js.
// One named export per operationId, populated in the implementation phase.
module.exports = {};
