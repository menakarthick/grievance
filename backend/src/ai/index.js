'use strict';

// Claude API client wrapper for AI classification/voice endpoints (Section 5): request
// construction, PII masking-before-egress and the deterministic fallback path
// (ARCHITECTURE.md §8.2-8.3). Consumed by src/services/ai.service.js.
// Populated in the implementation phase.
module.exports = {};
