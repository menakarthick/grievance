'use strict';

// Data-access layer for the File Management module — the only layer that talks
// to src/models directly. Keeps Sequelize query shapes out of the service
// layer.
module.exports = {};
