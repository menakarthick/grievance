'use strict';

// Cross-cutting audit log writer (10-Audit-APIs.md) — called from every module's
// service layer wherever an action is documented as auditable, not just the
// audit module's own read endpoints.
// Populated in the implementation phase.
module.exports = {};
